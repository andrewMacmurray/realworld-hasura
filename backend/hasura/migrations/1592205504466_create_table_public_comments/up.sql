CREATE TABLE "public"."comments"("id" serial NOT NULL, "article_id" integer NOT NULL, "user_id" integer NOT NULL, "comment" text NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") , FOREIGN KEY ("article_id") REFERENCES "public"."articles"("id") ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("user_id") REFERENCES "public"."users"("id") ON UPDATE cascade ON DELETE cascade);
